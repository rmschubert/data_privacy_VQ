[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "data_privacy_VQ"
version = "0.0.1"
requires-python = ">3.10"
dependencies = [
    "numpy",
    "scikit-image",
    "matplotlib",
    "rich",
    "pandas",
    "scikit-learn",
]